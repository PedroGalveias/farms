application:
  host: 0.0.0.0
database:
  require_ssl: true
session:
  secret_key: "${SESSION_SECRET_KEY}"
  cookie_secure: true
logging:
  level: "info"
  format: "bunyan"  # Bunyan-formatted JSON for production log aggregation
telemetry:
  enabled: true
  endpoint: "${OTEL_EXPORTER_OTLP_ENDPOINT}"  # Set via environment variable
  environment: "production"

#email_client:
#  base_url: "https://api.mailersend.com"
#  sender_email: "something@farms.com"
